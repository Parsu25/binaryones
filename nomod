#include<stdio.h>
#include <stdlib.h>
int main()
{
    int n,i,c=0,r;
    scanf("%d",&n);
    while(n>0)
    {
        r=n-2*(n/2);
        if(r==1)
        {
            c++;
        }
        n=n/2;    
    }
    printf("%d",c);
}
